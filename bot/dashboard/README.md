# Dashboard Structure

This folder is reserved for the optional web dashboard.

## Included
- `static/` for CSS/JS assets
- `templates/` for server-rendered pages

## Suggested Next Step
- Build OAuth2 Discord login flow.
- Add role-based session authorization.
- Render real-time ticket analytics from `/guilds/{guild_id}/dashboard` API endpoint.
